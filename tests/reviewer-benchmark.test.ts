import test from 'node:test';import assert from 'node:assert/strict';import {selectReviewerFleet} from '../src/reviewer-broker.js';
const now=new Date().toISOString();
test('reviewer selection uses task benchmark rather than generic popularity',()=>{const fleet=selectReviewerFleet([{id:'popular',provider:'openrouter',free:true,coding:.9,reasoning:.9,security:.9,context:128000,healthy:true,observedAt:now,source:'catalog',usageRank:1,benchmarks:[{benchmark:'sec',dimension:'security',score:.5,observedAt:now,source:'bench'},{benchmark:'reason',dimension:'reasoning',score:.7,observedAt:now,source:'bench'}]},{id:'secure',provider:'nvidia',free:true,coding:.8,reasoning:.8,security:.9,context:128000,healthy:true,observedAt:now,source:'catalog',usageRank:20,benchmarks:[{benchmark:'sec',dimension:'security',score:.95,observedAt:now,source:'bench'},{benchmark:'reason',dimension:'reasoning',score:.8,observedAt:now,source:'bench'}]}],['security']);assert.equal(fleet.assignments?.[0].modelId,'secure')});
test('unbenchmarked free model is not silently selected',()=>{const f=selectReviewerFleet([{id:'x',provider:'openrouter',free:true,coding:1,reasoning:1,security:1,context:1000000,healthy:true,observedAt:now,source:'catalog'}],['correctness']);assert.equal(f.models.length,0);assert.match(f.reason,/refresh/i)});