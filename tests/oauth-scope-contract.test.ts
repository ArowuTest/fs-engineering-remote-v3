import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('OAuth scopes are allowlisted and reviewer permissions include model review without write/node access',async()=>{const s=await fs.readFile(new URL('../src/oauth-http.ts',import.meta.url),'utf8');assert.match(s,/knownScopes=new Set\(\['fs\.read','fs\.write','fs\.node','fs\.models','fs\.admin'\]\)/);assert.match(s,/role==='reviewer'\?\['fs\.read','fs\.models'\]/);assert.match(s,/error:'invalid_scope'/);assert.match(s,/Invalid OAuth scope/)})
