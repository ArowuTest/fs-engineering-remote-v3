import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('compact GPT Actions resolve OAuth identity into workspace-scoped operations',async()=>{const s=await fs.readFile(new URL('../src/actions.ts',import.meta.url),'utf8');assert.match(s,/resolveOAuthAccessToken/);assert.match(s,/createRemoteOperations\(config,processes,workspaceId\)/);assert.match(s,/scope\(request/);assert.match(s,/fs\.write/);assert.match(s,/fs\.read/)})
