import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('provider API never lists secret envelopes',async()=>{const s=await fs.readFile(new URL('../src/user-platform-http.ts',import.meta.url),'utf8');assert.match(s,/\/api\/account\/providers/);assert.match(s,/SELECT provider,label,status,created_at,updated_at/);assert.doesNotMatch(s,/SELECT \* FROM provider_credentials/)})
