import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('invite acceptance and password reset completion are workspace-audited',async()=>{const http=await fs.readFile(new URL('../src/auth-http.ts',import.meta.url),'utf8');assert.match(http,/invitation\.accept/);assert.match(http,/password_reset\.consume/);const store=await fs.readFile(new URL('../src/auth-store.ts',import.meta.url),'utf8');assert.match(store,/return \{ok:true,userId:r\.rows\[0\]\.user_id,workspaceId:/)})
