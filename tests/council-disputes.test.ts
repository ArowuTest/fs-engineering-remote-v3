import test from 'node:test';import assert from 'node:assert/strict';import {sealCandidate,requiresFreshBlindCouncil} from '../src/council-seal.js';import {needsRechallenge,needsIndependentDispute,parseRechallenge,parseDispute} from '../src/council-disputes.js';
test('material candidate change invalidates prior council seal',()=>{const s=sealCandidate('a',{x:1},'2026-01-01T00:00:00Z');assert.equal(requiresFreshBlindCouncil(s,'a'),false);assert.equal(requiresFreshBlindCouncil(s,'b'),true)});
test('rejected material finding is privately rechallenged and maintained dispute escalates',()=>{const f:any={id:'f1',role:'security',severity:'high',claim:'x',adjudicationStatus:'REJECTED'};assert.equal(needsRechallenge(f),true);const r=parseRechallenge(f,JSON.stringify({verdict:'MAINTAIN',reason:'evidence remains'}));assert.equal(needsIndependentDispute(r),true);const d=parseDispute('f1',JSON.stringify({status:'INSUFFICIENT_EVIDENCE',reason:'needs deterministic proof'}));assert.equal(d.status,'INSUFFICIENT_EVIDENCE')});
