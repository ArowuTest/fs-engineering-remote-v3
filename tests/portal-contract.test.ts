import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('portal covers admin and invited-user flows',async()=>{const s=await fs.readFile(new URL('../src/portal.ts',import.meta.url),'utf8');for(const p of ['/portal/invite','/portal/reset','/portal/account','/portal/admin','Create invite','Reset password','Disable'])assert.match(s,new RegExp(p.replaceAll('/','\\/')))});