import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('Railway deployment is health checked and documents required hosted secrets',async()=>{const r=JSON.parse(await fs.readFile(new URL('../railway.json',import.meta.url),'utf8'));assert.equal(r.build.builder,'DOCKERFILE');assert.equal(r.deploy.healthcheckPath,'/healthz');const d=await fs.readFile(new URL('../docs/RAILWAY-DEPLOYMENT.md',import.meta.url),'utf8');for(const k of ['DATABASE_URL','FS_PUBLIC_BASE_URL','FS_GPT_OAUTH_CLIENT_SECRET','FS_HOSTED_GITHUB_TOKEN','FS_HOSTED_REPOSITORIES'])assert.match(d,new RegExp(k))})
