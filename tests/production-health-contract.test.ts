import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('hosted health fails closed when durable database is absent',async()=>{const s=await fs.readFile(new URL('../src/http.ts',import.meta.url),'utf8');assert.match(s,/RAILWAY_ENVIRONMENT/);assert.match(s,/FS_REMOTE_HOSTED === '1'/);assert.match(s,/database\.healthy \|\| \(!durableRequired && !database\.configured\)/)})
