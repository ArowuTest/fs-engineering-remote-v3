import test from 'node:test';import assert from 'node:assert/strict';import {remediationDirective,evidenceAfterDirective} from '../src/remediation.js';
test('remediation directive preserves findings factual basis and acceptance targets',()=>{const d=remediationDirective({id:'r1',findings:[{id:'sec-1',role:'security',severity:'high',claim:'missing auth',status:'confirmed'},{id:'bad',role:'security',severity:'high',claim:'false',status:'rejected'}],adjudication:{requiredActions:['add auth'],factualBasis:['route is public'],remainingUncertainty:['admin path']}},['unauthorized users are denied'],'2026-01-01T00:00:00Z');assert.deepEqual(d.findingIds,['sec-1']);assert.deepEqual(d.factualBasis,['route is public']);assert.equal(d.freshEvidenceRequired,true);assert.equal(d.acceptanceTargets[0],'unauthorized users are denied')});test('prior evidence cannot satisfy remediation freshness',()=>{const d=remediationDirective({id:'r',findings:[],adjudication:{}},[],'2026-01-02T00:00:00Z');const fresh=evidenceAfterDirective([{observedAt:'2026-01-01T00:00:00Z',kind:'test',status:'pass'},{observedAt:'2026-01-03T00:00:00Z',kind:'test',status:'pass'}],d);assert.equal(fresh.length,1);assert.equal(fresh[0].observedAt,'2026-01-03T00:00:00Z')});