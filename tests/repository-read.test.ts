import test from 'node:test';import assert from 'node:assert/strict';import {RepositoryRetrieval} from '../src/repository-retrieval.js';
test('targeted repository reads are bounded and idempotent',async()=>{const calls:any[]=[];const nodes={enqueue:async(a:any,o:any)=>{calls.push({a,o});return {id:String(calls.length)}}};const r=new RepositoryRetrieval(nodes as any);const paths=Array.from({length:30},(_,i)=>`src/f${i}.ts`);await r.read({missionId:'m',stepId:'s',nodeId:'n',project:'p',root:'fs',cwd:'.',query:'auth'},paths);assert.equal(calls.length,20);assert.equal(calls[0].a.capability,'filesystem');assert.match(calls[0].o.idempotencyKey,/retrieval-read:m:s/)});