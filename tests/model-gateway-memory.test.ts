import test from 'node:test';import assert from 'node:assert/strict';import {ModelGateway} from '../src/model-gateway.js';
test('gateway can route with refreshed in-memory catalog when database is absent',async()=>{const provider:any={complete:async(r:any)=>({provider:'openrouter',model:r.model,text:'ok'})};const catalog:any={list:async()=>[],upsert:async()=>{}};const g=new ModelGateway(provider,catalog);const old=globalThis.fetch;globalThis.fetch=(async()=>new Response(JSON.stringify({data:[{id:'free/test',pricing:{prompt:'0',completion:'0'},context_length:1000}]}),{status:200})) as any;try{const out=await g.complete({criticality:'mechanical',task:'test',system:'s',prompt:'p'});assert.equal(out.model,'openrouter::free/test')}finally{globalThis.fetch=old}});