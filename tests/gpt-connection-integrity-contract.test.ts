import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('GPT connection state can only be created by a completed OAuth exchange',async()=>{const platform=await fs.readFile(new URL('../src/user-platform-http.ts',import.meta.url),'utf8');assert.doesNotMatch(platform,/post\('\/api\/account\/gpt\/connect'/);const oauth=await fs.readFile(new URL('../src/oauth-http.ts',import.meta.url),'utf8');assert.match(oauth,/INSERT INTO oauth_connections/)})
