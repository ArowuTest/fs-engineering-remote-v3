import test from 'node:test';import assert from 'node:assert/strict';import {chunkReviewText} from '../src/council-packets.js';import {selectBlindReplacement} from '../src/council-replacement.js';
test('large review evidence is split into bounded packets',()=>{const x=chunkReviewText(('a'.repeat(10000)+'\n\n').repeat(10),43000);assert.ok(x.length>=3);assert.ok(x.every(c=>c.text.length<=43000));assert.equal(x.at(-1)?.total,x.length)});
test('blind replacement excludes failed model and preserves specialist role',()=>{const now=new Date().toISOString(),b=(id:string,security:number)=>({id,provider:'openrouter' as const,free:true,coding:.8,reasoning:.8,security,context:100000,healthy:true,observedAt:now,source:'test',benchmarks:[{benchmark:'sec',dimension:'security' as const,score:security,observedAt:now,source:'test'},{benchmark:'reason',dimension:'reasoning' as const,score:.8,observedAt:now,source:'test'}]});const r=selectBlindReplacement([b('failed',.99),b('replacement',.9)],'security',['failed']);assert.equal(r?.model.id,'replacement')});
