import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('auth HTTP exposes invite signup login admin and reset flows',async()=>{const s=await fs.readFile(new URL('../src/auth-http.ts',import.meta.url),'utf8');for(const p of ['/api/auth/signup','/api/auth/login','/api/auth/logout','/api/auth/me','/api/admin/users','/api/admin/invitations','password-reset'])assert.match(s,new RegExp(p.replaceAll('/','\\/')));assert.match(s,/owner','admin/)});