import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('settings UI covers model GPT and node controls',async()=>{const s=await fs.readFile(new URL('../src/portal.ts',import.meta.url),'utf8');for(const x of ['/portal/settings','Developer & council models','ChatGPT connection','Execution node','paid_if_consented'])assert.match(s,new RegExp(x.replaceAll('/','\\/')))})
