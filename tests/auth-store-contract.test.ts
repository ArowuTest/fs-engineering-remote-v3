import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('auth tokens are hash-only and reset revokes sessions',async()=>{const s=await fs.readFile(new URL('../src/auth-store.ts',import.meta.url),'utf8');assert.match(s,/hashOpaqueToken\(token\)/);assert.match(s,/FOR UPDATE/);assert.match(s,/UPDATE user_sessions SET revoked_at=now\(\) WHERE user_id/)});