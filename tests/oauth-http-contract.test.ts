import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('OAuth authorization code flow is redirect-bound single-use and hash-only',async()=>{const s=await fs.readFile(new URL('../src/oauth-http.ts',import.meta.url),'utf8');assert.match(s,/\/oauth\/authorize/);assert.match(s,/\/oauth\/token/);assert.match(s,/FOR UPDATE/);assert.match(s,/consumed_at=now/);assert.match(s,/allowedRedirect/);assert.match(s,/hashOpaqueToken\(access\)/);assert.match(s,/expires_in:3600/)})
