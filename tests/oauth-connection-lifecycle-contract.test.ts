import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('successful OAuth exchange records the real GPT client connection',async()=>{const oauth=await fs.readFile(new URL('../src/oauth-http.ts',import.meta.url),'utf8');assert.match(oauth,/INSERT INTO oauth_connections/);assert.match(oauth,/ON CONFLICT\(workspace_id,user_id,client_id\)/);assert.match(oauth,/x\.workspace_id,x\.user_id,clientId,x\.scopes/);const portal=await fs.readFile(new URL('../src/portal.ts',import.meta.url),'utf8');assert.doesNotMatch(portal,/connectGpt\.onclick/);assert.match(portal,/Start the connection from your Custom GPT/)})
