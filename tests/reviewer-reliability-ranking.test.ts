import test from 'node:test';import assert from 'node:assert/strict';import {selectReviewerFleet,type ReviewerModel} from '../src/reviewer-broker.js';import {recordModelFailure,resetModelHealth} from '../src/model-health.js';
const now=new Date().toISOString();const m=(id:string,score:number):ReviewerModel=>({id,provider:'openrouter',free:true,coding:score,reasoning:score,security:score,context:100000,healthy:true,observedAt:now,source:'test',benchmarks:[{benchmark:'sec',dimension:'security',score,observedAt:now,source:'test'},{benchmark:'reason',dimension:'reasoning',score,observedAt:now,source:'test'}]});
test('reliability breaks close benchmark ties',()=>{resetModelHealth();recordModelFailure('a',new Error('timeout'));const fleet=selectReviewerFleet([m('a',.91),m('b',.90)],['security']);assert.equal(fleet.models[0].id,'b')});
test('materially stronger benchmark competence outranks temporary reliability issue',()=>{resetModelHealth();recordModelFailure('strong',new Error('timeout'));const fleet=selectReviewerFleet([m('strong',.98),m('weak',.80)],['security']);assert.equal(fleet.models[0].id,'strong')});
