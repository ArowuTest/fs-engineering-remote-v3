import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('OAuth users cannot bypass compact workspace scopes through legacy granular Actions routes',async()=>{const s=await fs.readFile(new URL('../src/actions.ts',import.meta.url),'utf8');const marker=s.indexOf('// Compact GPT Actions surface.');assert.ok(marker>0);const legacy=s.slice(0,marker),compact=s.slice(marker);assert.match(legacy,/requireLegacyAuth/);for(const route of ['/actions/read-file','/actions/write-file','/actions/run-command','/actions/git-push','/actions/browser-start']){const i=legacy.indexOf(route);assert.ok(i>=0,route);assert.match(legacy.slice(i,i+120),/requireLegacyAuth/)}assert.match(compact,/preHandler: requireAuth/);assert.match(compact,/scopedOps\(request\)/);assert.match(compact,/scope\(request/)})
