import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('RemoteOperations constructs mission and worker stores with request workspace',async()=>{const s=await fs.readFile(new URL('../src/operations.ts',import.meta.url),'utf8');assert.match(s,/private readonly workspaceId\?: string/);assert.match(s,/MissionManager\(path\.join\(this\.runtime\.stateRoot,'missions'\), workspaceId\)/);assert.match(s,/WorkerQueue\(path\.join\(this\.runtime\.stateRoot,'work-queue'\),120000,workspaceId\)/)})
