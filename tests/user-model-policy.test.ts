import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('user model policy preserves free-first and explicit paid consent',async()=>{const s=await fs.readFile(new URL('../src/provider-user-store.ts',import.meta.url),'utf8');assert.match(s,/free_first/);assert.match(s,/Paid policy requires explicit consent/);assert.match(s,/developer-only/)});