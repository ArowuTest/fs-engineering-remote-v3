import test from 'node:test';import assert from 'node:assert/strict';import {validateCouncilPack} from '../src/council-pack-validation.js';
const base:any={product:{description:'Engineering portal'},mission:{id:'m',goal:'Build portal',root:'fs',cwd:'app'},step:{id:'s',title:'Secure API',acceptance:['tests pass'],attempts:1},candidate:'diff',capabilityContract:{},requirements:{overrides:[]},risk:{},truth:{known:[]},traceability:{requirements:[]},repositoryContext:{files:['src/a.ts']},evidence:[{kind:'test',source:'node:n1',status:'pass',summary:'tests pass',observedAt:'2026-08-23T02:00:00Z'}]};
test('canonical council pack records provenance and freshness',()=>{const v=validateCouncilPack(base,'testing');assert.equal(v.valid,true);assert.ok(v.facts.some(f=>f.path==='evidence[0]'&&f.source==='node:n1'&&!!f.observedAt));assert.equal(v.contextHash.length,64)});test('testing council refuses incomplete evidence pack',()=>{const v=validateCouncilPack({...base,evidence:[]},'testing');assert.equal(v.valid,false);assert.match(v.errors.join(' '),/test\/verification evidence/)});test('code-focused reviewer refuses missing repository context',()=>{const x={...base};delete x.repositoryContext;const v=validateCouncilPack(x,'security');assert.equal(v.valid,false);assert.match(v.errors.join(' '),/repositoryContext/)});