import test from 'node:test';import assert from 'node:assert/strict';import {ReviewCouncil} from '../src/council.js';
class P{n=0;async complete(){this.n++;if(this.n===1)return {provider:'x',model:'x',text:JSON.stringify({verdict:'block',summary:'security',findings:[{severity:'high',location:'a.ts:1',claim:'auth bypass'}]}),requestId:'1'};if(this.n===2)return {provider:'x',model:'x',text:JSON.stringify({verdict:'approve',summary:'ok',findings:[]}),requestId:'2'};if(this.n===3)return {provider:'x',model:'x',text:JSON.stringify({verdict:'uncertain',reason:'insufficient proof'}),requestId:'3'};return {provider:'x',model:'x',text:JSON.stringify({verdict:'approve',summary:'approve',requiredActions:[]}),requestId:'4'}}}
test('council fails closed when skeptic is uncertain about high blocker',async()=>{const r=await new ReviewCouncil(new P() as any).run({missionId:'m',goal:'g',candidate:'c',evidence:{},roles:['security','testing']});assert.equal(r.findings[0].status,'uncertain');assert.equal(r.adjudication.verdict,'block')});