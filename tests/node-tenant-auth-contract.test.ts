import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('node self-service endpoints derive workspace from node credentials before accessing jobs',async()=>{const http=await fs.readFile(new URL('../src/node-http.ts',import.meta.url),'utf8');const nodes=await fs.readFile(new URL('../src/nodes.ts',import.meta.url),'utf8');assert.match(nodes,/static async resolveIdentity/);assert.match(nodes,/credential_hash=\$3/);for(const route of ['heartbeat','claim','complete']){const i=http.indexOf(`/node/${route}`);assert.ok(i>=0);assert.match(http.slice(i,i+700),/resolveIdentity/);assert.match(http.slice(i,i+700),/new NodeRegistry\(identity\.workspaceId\)/)}})
