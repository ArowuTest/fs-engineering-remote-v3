import test from 'node:test';import assert from 'node:assert/strict';import {ProviderHealthRegistry} from '../src/provider-health.js';import {researchRequest,researchEvidence} from '../src/research-broker.js';import {missingReliabilityRuns,reliabilityGate} from '../src/reliability.js';
test('provider circuit opens after bounded failures',()=>{const h=new ProviderHealthRegistry(2,10000);h.failure('x');assert.equal(h.canUse('x'),true);h.failure('x');assert.equal(h.canUse('x'),false);h.success('x');assert.equal(h.canUse('x'),true)});test('research becomes evidence only when authority threshold is met',()=>{const r=researchRequest('current gambling law','GB'),e=researchEvidence('m','s',r,[{url:'https://a',title:'A',authority:'regulator_government',observedAt:new Date().toISOString(),independentKey:'a',claim:'x'}]);assert.equal(e.status,'unknown')});test('pass^k reports exact missing consecutive runs',()=>{const g=reliabilityGate('production auth');assert.equal(missingReliabilityRuns(g,[true,false,true]),2);assert.equal(missingReliabilityRuns(g,[true,true,true]),0)});