import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';test('Railway deployment documents mandatory config secrets required before app can boot',async()=>{const config=await fs.readFile(new URL('../src/config.ts',import.meta.url),'utf8');const docs=await fs.readFile(new URL('../docs/RAILWAY-DEPLOYMENT.md',import.meta.url),'utf8');for(const name of ['FS_REMOTE_ENDPOINT_SECRET','FS_REMOTE_ACTIONS_SECRET']){assert.match(config,new RegExp(name));assert.match(docs,new RegExp(name))}assert.match(config,/actionsSecret must differ from endpointSecret/)})
