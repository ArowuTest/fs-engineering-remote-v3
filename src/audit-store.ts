import {db} from './db.js';
export class AuditStore{async write(x:{workspaceId:string;actor:string;action:string;targetType?:string;targetId?:string;payload?:Record<string,unknown>}){await db().query(`INSERT INTO audit_events(instance_id,actor,action,target_type,target_id,payload,workspace_id) VALUES($1,$2,$3,$4,$5,$6,$7)`,[process.env.FS_REMOTE_INSTANCE_ID??'v3-default',x.actor,x.action,x.targetType??null,x.targetId??null,x.payload??{},x.workspaceId])}async list(workspaceId:string,limit=100){return (await db().query(`SELECT id,actor,action,target_type,target_id,payload,created_at FROM audit_events WHERE workspace_id=$1 ORDER BY created_at DESC LIMIT $2`,[workspaceId,Math.min(Math.max(limit,1),250)])).rows}}
