import {SkillCatalog,type SkillEvaluation} from './skills.js';
export interface SkillSelection{query:string;selected:{id:string;name:string;description:string;evaluation:SkillEvaluation;resources:string[]}[];rejected:{id:string;reason:string}[]}
export class SkillRouter{constructor(private readonly catalog:SkillCatalog){}async select(queries:string[],limit=8):Promise<SkillSelection>{const candidates=new Map<string,Awaited<ReturnType<SkillCatalog['list']>>[number]>();for(const q of queries){for(const s of await this.catalog.list(q,undefined,20))candidates.set(s.id,s)}const selected:SkillSelection['selected']=[],rejected:SkillSelection['rejected']=[];for(const s of candidates.values()){const evaluation=await this.catalog.evaluate(s.id);if(evaluation.verdict==='reject'){rejected.push({id:s.id,reason:evaluation.findings.join('; ')||'skill rejected by quality gate'});continue}const resources=(await this.catalog.listResources(s.id)).map(r=>r.path);selected.push({id:s.id,name:s.name,description:s.description,evaluation,resources});if(selected.length>=limit)break}return {query:queries.join(' | '),selected,rejected}}
}