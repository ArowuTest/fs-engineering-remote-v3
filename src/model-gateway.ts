import type {ReasoningProvider,ReasoningRequest,ReasoningResponse} from './reasoning.js';import {recordModelFailure,recordModelSuccess} from './model-health.js';import {reasoningProvider} from './reasoning.js';import {ReviewerCatalogStore} from './reviewer-catalog.js';import {refreshReviewerCatalog} from './reviewer-source.js';import {reviewerCatalogNeedsRefresh,type ReviewerModel} from './reviewer-broker.js';import {routeModel,type TaskCriticality} from './model-routing-policy.js';
export interface GovernedReasoningRequest extends ReasoningRequest{criticality:TaskCriticality;task:string;configuredModelId?:string;paidModelConsent?:boolean}
export class ModelGateway{private memoryCatalog:ReviewerModel[]=[];constructor(private readonly provider:ReasoningProvider=reasoningProvider(),private readonly catalog=new ReviewerCatalogStore()){}
 async availableModels():Promise<ReviewerModel[]>{let models=await this.catalog.list();if(!models.length&&this.memoryCatalog.length)models=this.memoryCatalog;if(reviewerCatalogNeedsRefresh(models)){try{const fresh=await refreshReviewerCatalog();this.memoryCatalog=fresh.models;await this.catalog.upsert(fresh.models);models=await this.catalog.list();if(!models.length)models=this.memoryCatalog}catch(error){if(!models.length)throw new Error(`Model routing catalog unavailable: ${error instanceof Error?error.message:String(error)}`)}}return models}
 async complete(req:GovernedReasoningRequest):Promise<ReasoningResponse&{routing:unknown}>{const models=await this.availableModels();const configured=req.configuredModelId??req.model,decision=routeModel(models,{criticality:req.criticality,configuredModelId:configured,paidModelConsent:req.paidModelConsent,task:req.task});if(decision.requiresConsent)throw new Error(`PAID_MODEL_CONSENT_REQUIRED: ${decision.reason}`);if(!decision.model)throw new Error(`MODEL_ROUTING_UNAVAILABLE: ${decision.reason}`);const model=`${decision.model.provider}::${decision.model.id}`;let out:ReasoningResponse;try{out=await this.provider.complete({...req,model});recordModelSuccess(decision.model.id)}catch(error){recordModelFailure(decision.model.id,error);throw error}return {...out,routing:{...decision,model:{id:decision.model.id,provider:decision.model.provider,free:decision.model.free},task:req.task,criticality:req.criticality}}}}
export function modelGateway(){return new ModelGateway()}