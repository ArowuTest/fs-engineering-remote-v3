import {db,databaseEnabled} from './db.js';import type {ReviewerModel} from './reviewer-broker.js';
export class ReviewerCatalogStore{
 async list():Promise<ReviewerModel[]>{if(!databaseEnabled())return [];const r=await db().query('SELECT * FROM reviewer_catalog WHERE healthy=true ORDER BY observed_at DESC');return r.rows.map((x:any)=>({id:x.model_id,provider:x.provider,free:x.free,coding:Number(x.metadata?.coding??0),reasoning:Number(x.metadata?.reasoning??0),security:Number(x.metadata?.security??0),context:Number(x.metadata?.context??0),healthy:x.healthy,observedAt:new Date(x.observed_at).toISOString(),source:x.source,benchmarks:x.benchmarks??[],usageRank:x.metadata?.usageRank}))}
 async upsert(models:ReviewerModel[]){if(!databaseEnabled())return;for(const m of models)await db().query(`INSERT INTO reviewer_catalog(model_id,provider,free,healthy,observed_at,source,metadata,benchmarks) VALUES($1,$2,$3,$4,$5,$6,$7,$8) ON CONFLICT(model_id) DO UPDATE SET provider=EXCLUDED.provider,free=EXCLUDED.free,healthy=EXCLUDED.healthy,observed_at=EXCLUDED.observed_at,source=EXCLUDED.source,metadata=EXCLUDED.metadata,benchmarks=EXCLUDED.benchmarks`,[m.id,m.provider,m.free,m.healthy,m.observedAt,m.source,{coding:m.coding,reasoning:m.reasoning,security:m.security,context:m.context,usageRank:m.usageRank},m.benchmarks??[]])}
}