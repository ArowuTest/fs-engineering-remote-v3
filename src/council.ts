import crypto from 'node:crypto';import {reasoningProvider,type ReasoningProvider} from './reasoning.js';
export interface CouncilReview{role:string;model?:string;verdict:'approve'|'changes'|'block';summary:string;findings:string[];raw:string}
const roles=['requirements','security','correctness','testing','hardening','architecture'];
function parse(role:string,raw:string):CouncilReview{let x:any;try{x=JSON.parse(raw.match(/\{[\s\S]*\}/)?.[0]??raw)}catch{};const verdict=['approve','changes','block'].includes(x?.verdict)?x.verdict:'changes';return {role,verdict,summary:String(x?.summary??'Reviewer response requires adjudication.'),findings:Array.isArray(x?.findings)?x.findings.map(String):[],raw}}
export class ReviewCouncil{constructor(private readonly provider:ReasoningProvider=reasoningProvider()){}
 async run(input:{missionId:string;goal:string;candidate:string;evidence:unknown;roles?:string[];models?:string[]}){const selected=input.roles?.length?input.roles:roles;const reviews:CouncilReview[]=[];for(let i=0;i<selected.length;i++){const role=selected[i];const out=await this.provider.complete({model:input.models?.[i%Math.max(1,input.models?.length??1)],system:`You are the blind ${role} reviewer in a software engineering review council. Judge only supplied requirements, candidate change summary and deterministic evidence. Return JSON {verdict:"approve|changes|block",summary:string,findings:string[]}.`,prompt:JSON.stringify({goal:input.goal,candidate:input.candidate,evidence:input.evidence})});reviews.push(parse(role,out.text))}const adjudication=await this.provider.complete({system:'You are the independent adjudicator of a software engineering review council. Deterministic evidence outranks reviewer speculation. Return JSON {verdict:"approve|changes|block",summary:string,requiredActions:string[]}.',prompt:JSON.stringify({missionId:input.missionId,reviews:reviews.map(({raw,...r})=>r),evidence:input.evidence})});let decision:any;try{decision=JSON.parse(adjudication.text.match(/\{[\s\S]*\}/)?.[0]??adjudication.text)}catch{decision={verdict:'changes',summary:'Adjudicator output was not structured.',requiredActions:[]}}return {id:`council-${Date.now()}-${crypto.randomBytes(3).toString('hex')}`,reviews,adjudication:decision,rawAdjudication:adjudication.text}}
}