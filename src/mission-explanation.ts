import type {Mission,MissionStep,Evidence} from './missions.js';import {missingProof} from './traceability.js';
export function explainMission(mission:Mission,step:MissionStep,evidence:Evidence[]){const proof=missingProof(mission,step,evidence),fresh=evidence.filter(e=>Date.now()-Date.parse(e.observedAt)<=6*60*60*1000),passed=fresh.filter(e=>e.status==='pass'),failed=fresh.filter(e=>e.status==='fail');return {status:mission.status,step:{id:step.id,title:step.title,status:step.status},known:passed.map(e=>({kind:e.kind,summary:e.summary,source:e.source,observedAt:e.observedAt})),failed:failed.map(e=>({kind:e.kind,summary:e.summary,source:e.source,observedAt:e.observedAt})),unknown:[...proof.missingEvidence.map(x=>`Missing ${x} evidence`),...proof.unverifiedAcceptance.map(x=>`Unverified acceptance: ${x}`)],contradictions:proof.contradictions,ready:proof.ready,next:proof.ready?'Independent review/adjudication may proceed.':'Collect the missing objective evidence or resolve contradictions before completion.',principle:'This explanation distinguishes observed evidence from assumptions, memory, model reasoning and unverified claims.'}}
